<!--pages/home/childCpns/w-recommend/w-recommend.wxml-->
<view class="recommend">
  <block wx:for="{{list}}" wx:key="{{index}}">
    <view class="recommend-item">
      <image src="{{item.image}}" bind:load="handImageLoad"/>
     <view>{{item.title}}</view>
    </view>
  </block>
</view>
